#include <REGX52.H>
#include "Delay.h"
#include "MatrixLED.h"
//此处定义的code关键字能够使得该数据存储到flash中，但是数组中的数据是不可更改的，避免过多占用RAM的空间
unsigned char code Animation[]={
	0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,
	0xFF,0x01,0x01,0x01,0x70,0x89,0x89,0x66,
	0x00,0xFF,0x08,0x08,0x08,0xFF,0x00,0x00,
	0xDF,0x00,0x00,0x7E,0x81,0x81,0x81,0x7E,
	0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,
	0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00
};
void main()
{
	unsigned char i,Offset=0,Count=0;
	MatrixLED_Init();
	while(1)
	{
		for(i=0;i<8;i++)
		{
			MatrixLED_ShowColumn(i,Animation[i+Offset]);
		}
		Count++;
		//Count计数起到一个延时的作用 这里是计数10次后才播放下一帧
		if(Count>10)
		{
			Count=0;
			Offset++;
			//偏移量需要置零防止下标越界
			if(Offset>40)
			{
				Offset=0;
			}
		}
	}
}